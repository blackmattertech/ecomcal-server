PORT=4000
# Fee tables: local (fast, default) | supabase (always hit DB)
FEE_SOURCE=local
# Optional — used for background refresh when FEE_SOURCE=local
SUPABASE_URL=
SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# FEE_CACHE_TTL_MS=900000
